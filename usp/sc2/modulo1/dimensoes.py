def dimensoes (matriz):
    lin = 0
    col = 0
    lin = len (matriz)
    for linha in matriz:
        lista_da_linha = linha
        col = len (lista_da_linha)
    return print(lin,"X",col, sep="")
        